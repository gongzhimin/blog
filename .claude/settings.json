{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/zhimin/Workshop/Sieve/MyBlog/blog && git add -A && git diff --cached --quiet && exit 0 || git commit -m \"$(TZ=Asia/Shanghai date '+[checkpoint] %Y-%m-%d %H:%M')\" 2>/dev/null; exit 0"
          }
        ]
      }
    ]
  }
}
